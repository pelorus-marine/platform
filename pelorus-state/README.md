# pelorus-state

**Pelorus State** scaffold: fusion and policy over **semantics**-aligned Core and Stream data. Optional **`stream`** feature pulls in **`pelorus-stream`** for envelope types.

Workspace root: `../README.md`.
