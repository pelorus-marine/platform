[workspace]
resolver = "2"
members = [
  "dbc-rs",
  "mdf4-rs",
  "pelorus-stream",
  "pelorus-state",
  "pelorus-core",
]
default-members = ["pelorus-core"]

# Safe Rust only for every member that sets `[lints] workspace = true`.
[workspace.lints.rust]
unsafe_code = "forbid"
